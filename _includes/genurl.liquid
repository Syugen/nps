{% assign arr = "" | split: ',' %}
{% assign post = page %}
{% for i in (1..page.total) %}
  {% assign post = post.next %}
  {% assign url = post.url | relative_url %}
  {% assign arr = arr | push: url %}
{% endfor %}
