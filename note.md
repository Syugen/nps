---
layout: post
title: Codex 对 NPS 站点的改动记录
permalink: /notes/
---

范围：本任务及其相邻的两个 NPS Codex 任务中实际完成的网站与工具改动。不记录文章正文撰写、主题/托管方案的纯讨论，已撤销的尝试也不列入。日期为任务记录中的大致日期。

## 页面布局与基础样式

### 固定的宽屏阅读布局

时间：大致 2026-09-01。

将宽屏布局定为左栏 200px、栏间距 50px、正文 800px；窗口达到 1150px 时才切换为左右两栏，整体居中。低于该宽度时标题栏在上、正文在下，避免原先中等宽度下左右栏重叠或溢出。

### 正文阅读规格

时间：大致 2026-09-01。

正文统一为 16px、较深的灰黑色，最大阅读宽度为 800px。

### 头像

时间：大致 2026-09-01。

宽屏左栏头像按左栏宽度显示；窄屏时设置了最大宽度。

### 样式文件拆分与注释

时间：大致 2026-08-31 至 2026-09-01。

将评论区专用的颜色、表单、按钮、弹窗、加载状态和动画拆入独立的 `comments.scss`，主样式只负责引入。新增的布局 Sass block 上方补充中文说明，解释作用对象、断点和复杂选择器的用途。

## 文章内图片展示

### 单图和双图的响应式规则

时间：大致 2026-09-01 至 2026-09-05。

普通图片不会超过正文列，并会随窄屏继续缩小。并排图片按正文实际可用宽度，而不是浏览器固定宽度决定何时换行：当两张图会被压到约 250px 以下时，改为单独一行、居中显示，并在窄屏采用统一的 400px 上限。

### 未指定宽度的独占图尺寸

时间：大致 2026-09-05。

宽屏时，所有未设置 `w` 的独占图都按原始比例计算显示尺寸：宽度介于 400px 和 800px 之间，且尽量限制高度不超过视口的 2/3。窄屏则与其他图片一样可小于 400px，避免横向溢出或变形；不再以“高大于宽的 1.2 倍”区分竖图。

### 更简洁的双图标记

时间：大致 2026-09-05。

`image.html` 使用 `hf=1` 标记并排图：同一段中连续两张会自动排为左、右两图。由于 Jekyll include 参数必须写成“名称=值”，不能裸写 `hf`，应写 `hf=1`；所有文章图片引用均使用这一写法。

### 图片 include 清理

时间：大致 2026-09-05。

双图的尺寸、浮动和间距由 class 与 Sass 集中控制，不再依赖旧的 HTML `width`、`align` 或内联 style；图片外层的多余 `p` 包装也已清理。

## 图片交互与画廊

### 悬停放大预览

时间：大致 2026-09-01 至 2026-09-05。

鼠标停在文章图片上时，原图保持不动，页面上层显示一张放大预览图。预览对齐正文范围，已满宽的图片不会重复放大；展开/收回使用短动画。根据图片在视口上、中、下的位置决定向下、居中或向上展开，并确保预览留在视口内。

放大预览图加一个细白边，使相邻图片更容易分辨而不改变图片显示大小。

修复了快速重新进入图片时旧预览可能残留的竞态问题；滚动、调整窗口或鼠标离开时会关闭预览。滚动到图片后，即使鼠标原本已经停在图片上，只要仍在其范围内移动也可触发预览。

### 可分享的单图画廊

时间：2026-09-03 至 2026-09-05。

新增 `gallery.html` 与 `assets/gallery.js`。点击文章图片会进入只展示一张图的独立画廊页，可通过上一张/下一张浏览同一篇文章中的图片。画廊 URL 用 `post` 与 `image` 参数定位内容，因此能直接分享；页面标题为“画廊：文章标题”，并复用 favicon。

初始图片居中且限制在视口内；如果原图大于视口，点击可显示原尺寸并滚动查看。翻页组件固定在视口中间偏下，鼠标移动时渐显、静止后渐隐；窄屏仅在必要时压缩按钮栏。

## 主页目录

### 长目录自动分两栏

时间：大致 2026-09-05。

新增 `assets/toc-columns.js`。当正文宽度约为 800px 且一个目录区段超过 6 行 bullet（所有嵌套层级均计入，不计自然换行）时，目录在一级条目边界分为尽量均衡的两栏；无法均分时左栏多一项，二级条目不会被拆开。窄屏仍是一栏。

## WebP 与图片资产

### 图片目录比对与缓存

时间：大致 2026-09-04。

由于本 repo 之前只包含 jpeg 格式的小图，我想找到所有的原图并制作 webp 格式的略大一点的图。我先在旅游照片原图的相册里挑选出图片到一个文件夹。为了保障新生成的 webp 与原来的 jpeg 比例相同，利用 Codex 做了这个任务：

新增 `compare-image-folders.ps1`，递归比对两套图片目录：用不含扩展名的相对路径匹配文件，报告缺失目录/文件并比较显示后的长宽比。右侧目录不存在时直接跳过整棵左侧子树；输出包含左右文件、后缀、尺寸、orientation 和比例，方便定位错误来源。

脚本为成功比对的结果写入右侧目录的 JSON 缓存，以相对路径和文件状态摘要判断是否可跳过；缓存命中会明确打印跳过原因，避免重复解码慢速 HEIC。另有单文件诊断流程可读取 JPG、JPEG、HEIC 的宽高和 orientation 元数据。

### JPEG / WebP 人工对照页

时间：大致 2026-09-04。

由于人工找到所有原图可能存在失误选错图的情况，利用 Codex 做了这个任务：

新增 `generate-jpeg-webp-comparison.py` 和生成的 `jpeg-webp-comparison.html`。它按相对路径配对 `images/jpeg` 与 `images/webp`，每行左右展示 JPEG 和 WebP；单侧缺失则留空，便于确认两者是否来自同一张图片。顶部数据由生成脚本写入，并非浏览器动态扫描。

### WebP 批量生成

时间：大致 2026-09-04。

新增 `generate-webp.ps1`，使用 ImageMagick 的 `magick` 递归生成 WebP，保留相对路径，不放大小图，已有输出不重复生成。生成规格曾区分 thumb（底边 800px）与 large（侧边 1600px）；当前网页展示统一使用 `images/webp/` 中的大图。缩略图目前没有使用。

### 文章图片引用迁移

时间：大致 2026-09-04 至 2026-09-05。

编写脚本扫描文章中的图片 include，并按对应 WebP 文件改写引用。
